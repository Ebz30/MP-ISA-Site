[build]
  # Ensure Netlify installs dependencies for the subproject, then build
  command = "npm --prefix mpunga-isa ci && npm --prefix mpunga-isa run build"
  publish = "mpunga-isa/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
